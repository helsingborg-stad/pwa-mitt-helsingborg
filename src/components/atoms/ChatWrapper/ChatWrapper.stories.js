import React from 'react';
import ChatWrapper from './ChatWrapper';

export default {
  title: 'ChatWrapper',
  component: ChatWrapper,
};

export const Default = () => <ChatWrapper>Hello ChatWrapper</ChatWrapper>;
