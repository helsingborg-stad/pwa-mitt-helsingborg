import React from 'react';
import ChatSubmitButton from './ChatSubmitButton';

export default {
  title: 'ChatSubmitButton',
  component: ChatSubmitButton,
};

export const Default = () => <ChatSubmitButton />;
